Read AGENTS.md before changing anything in this repo. It holds the invariants
and design rules. AI-HANDOVER.md and docs/RECIPES.md describe every common
task step by step. Definition of done: `npx astro check` reports 0 errors and
`npm run ship` passes every gate. Never invent facts to fill the gaps in TODO.md.
