# Directus extensions

Ace Games-specific Directus extensions can be mounted here. The first release
uses schema, policies and provider-neutral scripts from `scripts/cms/`, so no
custom authentication, CRUD table or media-library extension is required.

