{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "www.serviceAccountName" . }}
  labels:
    {{- include "www.labels" . | nindent 4 }}
# The pods talk to nobody but the visitor; the token is not mounted.
automountServiceAccountToken: false
{{- end }}
